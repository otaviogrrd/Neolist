// Use for all controller of application.
var appControllers = angular.module('neolist.controllers', []); 

// Use for all service of application.
var appServices = angular.module('neolist.services', []);

// Use for all directivess of application.
var appDirectives = angular.module('neolist.directives', []);

// Use for all factories of application.
var appFactories = angular.module('neolist.factories',[]);
